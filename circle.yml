machine:
  services:
    - docker
    - postgresql
    - redis

dependencies:
  override:
    - docker info
    - docker build -t unicef/equitrack .

test:
  override:
    - docker run --entrypoint --env DJANGO_SETTINGS_MODULE=EquiTrack.settings.test unicef/equitrack EquiTrack/manage.py test

