machine:
  services:
    - docker
    - postgresql
    - redis

dependencies:
  override:
    - docker info
    - docker build -t unicef/equitrack .

test:
  override:
    - docker run -e "DJANGO_SETTINGS_MODULE=EquiTrack.settings.test" --entrypoint unicef/equitrack EquiTrack/manage.py test

