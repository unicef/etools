machine:
  services:
    - docker
    - postgresql

dependencies:
  override:
    - docker info
    - docker build -t unicef/equitrack .

test:
  override:
    - docker run --env DJANGO_SETTINGS_MODULE=EquiTrack.settings.test unicef/equitrack EquiTrack/manage.py test

